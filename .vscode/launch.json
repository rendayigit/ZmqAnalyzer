{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Zmq_Analyzer",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/bin/Zmq_Analyzer",
      "args": [],
      "cwd": "${workspaceFolder}/bin/",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ]
    }
  ]
}
